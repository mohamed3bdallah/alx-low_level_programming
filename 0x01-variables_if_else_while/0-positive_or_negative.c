#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;
if (n 0)
{
printf("is zero");
}
if else (n > 0)
{
printf("is negative");
}
else
{
printf("is positive");
}
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	return (0);
}
